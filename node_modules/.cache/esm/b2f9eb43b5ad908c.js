let getTokens,camelCase;_b5a‍.x([["default",()=>_b5a‍.o]]);_b5a‍.w("../utils",[["getTokens",["getTokens"],function(v){getTokens=v}],["camelCase",["camelCase"],function(v){camelCase=v}]]);

const getTypography = (layerName, stylesArtboard) => {
  const palette = {typography: {}}
  const decorator = element => {
    const {name} = element
    const {
      fontFamily,
      fontSize,
      lineHeightPx,
      fontWeight
    } = element.children[0].style

    const tokens = {
      [camelCase(name)]: {
        fontFamily: {value: `'${fontFamily}'`},
        fontSize: {value: `${fontSize}px`},
        lineHeight: {value: `${Math.floor(lineHeightPx)}px`},
        fontWeight: {value: fontWeight}
      }
    }
    Object.assign(palette.typography, tokens)
  }

  return getTokens(layerName, stylesArtboard, palette, decorator)
}

_b5a‍.d(getTypography);
