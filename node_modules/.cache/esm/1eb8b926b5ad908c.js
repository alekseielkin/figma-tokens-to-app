let getTokens,camelCase,genShadow;_b5a‍.x([["default",()=>_b5a‍.o]]);_b5a‍.w("../utils",[["getTokens",["getTokens"],function(v){getTokens=v}],["camelCase",["camelCase"],function(v){camelCase=v}],["genShadow",["genShadow"],function(v){genShadow=v}]]);

const getShadows = (layerName, stylesArtboard) => {
  const palette = {shadow: {}}
  const decorator = element => {
    const {name} = element
    const {color, offset, radius} = element.effects[0]
    const tokens = {
      [camelCase(name)]: {value: genShadow(color, offset, radius)}
    }
    Object.assign(palette.shadow, tokens)
  }

  return getTokens(layerName, stylesArtboard, palette, decorator)
}

_b5a‍.d(getShadows);
